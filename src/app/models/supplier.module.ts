export interface SupplierRequest {
  name: string;
  contact: string;
  rating: number;
  leadTime: number;
}

export interface SupplierResponse {
  id: number;
  name: string;
  contact: string;
  rating: number;
  leadTime: number;
}
